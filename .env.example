# WF Intelligence Chatbot — Required Credentials
# Copy to .env and fill in values

# Pusher (free tier: pusher.com)
PUSHER_APP_ID=
PUSHER_KEY=
PUSHER_SECRET=
PUSHER_CLUSTER=ap2

# imgBB (optional — free image hosting for charts)
IMGBB_API_KEY=
